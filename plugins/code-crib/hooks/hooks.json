{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "SILENT CHECK: If this session had significant work (code edits, bug fixes, new features, architecture decisions), auto-save via /stash command. If NO significant work detected (only file reads, config changes, questions/answers, exploration), respond with ONLY this exact JSON: {\"decision\": \"skip\"}. Do not add any explanation or extra text."
          }
        ]
      }
    ]
  }
}
